export const TOKEN_REFRESH_BUFFER_MS = 1 * 60 * 1000; // 1 minutes
export const ACCESS_TOKEN_STORAGE_KEY = "auth-store";
export const IDLE_LOGOUT_TIMEOUT_MS = 2 * 60 * 1000; // 2 minutes
export const DEFAULT_AUTH_REDIRECT = "/login";
export const IDLE_DEBOUNCE_MS = 500;
